---
const { class: className, id } = Astro.props;
type Props = {
  class?: string;
  id?: string;
};
---

<section
  id={id}
  class:list={[
    "home-section w-full  flex flex-col items-center justify-center min-h-[calc(100vh-40px)]  text-gray-100",
    className,
  ]}
>
  <slot />
</section>
