---
const { title, description } = Astro.props;
type Props = {
  title: string;
  description: string;
};
---

<div class="card">
  <h3 class="text-xl font-semibold py-2">{title}</h3>
  <p class="text-lg">
    {description}
  </p>
</div>
